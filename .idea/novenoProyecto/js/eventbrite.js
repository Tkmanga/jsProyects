class Eventbrite{
    constructor() {
        this.toke_auth = `W2T37WJXY2UZYDY7UQ`;
        this.ordenar = 'date';
    }

    //obtiene las categorias en init

    async obtenerCategorias(){
        //consultar las categorias a las rest api
        const url = ``;
        const objeto = await (url);
    }
}